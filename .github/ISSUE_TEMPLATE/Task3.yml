name: Backend Component
description: Manage a backend component for the project
title: '[feat] First API
labels: ['backend']
assignees:
  - AdamsGeeky
body:
  - type: textarea
    attributes:
      label: Component Description
      description: Describe the purpose and functionality of the backend component.
      placeholder: Explain what this component is responsible for.
      value: |
        Create an Express server and load routes from `routes/index.js`.
  - type: textarea
    attributes:
      label: API Routes
      description: Describe API routes together with purpose and functionality
      value: |
        - Server should listen on the port set by the `PORT` environment variable or default to 5000.
        - Create `GET /status` and `GET /stats` endpoints in `AppController`.
          - `GET /status`: Return `{ "redis": true, "db": true }`.
          - `GET /stats`: Return `{ "users": 12, "files": 1231 }`.
    validations:
      required: false
  - type: textarea
    attributes:
      label: Data Flow
      description: Describe data flow together with purpose and functionality
      value: |
        - Data is received from clients via API requests.
        - The component processes the data and interacts with the database.
        - Processed data is returned to clients as API responses.
    validations:
      required: false
  - type: textarea
    attributes:
      label: Additional context
      description: Add any other context or screenshots about the feature request here.
    validations:
      required: false
